LENGTH OF THE STRING WITHOUT STRING FUNCTION


#include <stdio.h>
#include<string.h>

int main() {
   char str[50];
   int c=0;
  fgets(str,50,stdin);
  for(int i=0;str[i]!='\0';i++)
   {
       c++;
   }
   printf("%d",c);

    return 0;
}
