COUNT THE SUM OF DIGITS HACKERRANK'

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
char str1[50];
    int sum=0;
    scanf("%[^\n]s",str1);
    int i;
    for(i=0;str1[i]!='\0';i++){
        if(str1[i]>=48 && str1[i]<=57){
            sum=sum+str1[i]-48;
        }
    }
    printf("%d",sum);
     
    
    return 0;
}
