STRING COMPARE

#include <stdio.h>
#include<string.h>

int main() {
    char str1[50],str2[50];
   
    scanf("%s",str1);
    scanf("%s",str2);
    int s=strcmp(str1,str2);
    if(s==0){
      printf("Strings are equal");  
    }
    else{
    printf("strings are not equal");
}
    
}
